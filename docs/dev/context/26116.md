## Gateway Session — 2026-03-15

**Branch:** feature/gateway-pubkey
**Build:** clean

### Completed
- Dropped `event_type` from EventRecord, DB schema, TUI header/rows/constraints
- Extracted `ble_task()` from main() spawn closure
- Dropped unused `lima_types` imports (NONCE_LEN, TAG_LEN, OUTER_SIG_LEN)
- Explicit `as i16` cast for rssi
- Removed low-value inline comments

### Wire format confirmed from live firmware log
- ADV blob: 90 bytes
- [0..4]  header (company_id + proto_ver + event_type)
- [4..8]  sequence
- [8..12] timestamp_ms
- [12..16] accel_g
- [16..20] delta_pa
- [20..26] node_id
- [26..90] sig (64 bytes r||s raw)
- verify_outer_sig() reconstructs lima_payload_t from ADV layout — confirmed correct

### Blocking
- BLE scanner not picking up node — bluetoothctl scan showing nothing
- Needs hardware triage next session (adapter, node powered, range)

### Next
- Resolve BLE scan issue
- Confirm green checkmark in TUI
- Commit: feat(gateway): verify_outer_sig() live
- Then: MQTT pipeline (v0.2 branch)